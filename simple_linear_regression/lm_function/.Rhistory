str(cstr())
str(cars)
stop_dist_model=lm(dist~speed, data= cars)
stop_dist_model
plot(dist~speed, data= cars, xlab = " Speed (in Miles per Hour)", ylab = " Stopping Distance (in Foot)", main = " Stopping Distance vs speed", pch = 20, cex = 2, col = "grey")
abline(stop_dist_model, lwd = 2, col = "purple")
names(stop_dist_model)
summary(stop_dist_model)
x=sqrt(0.6511)
y=x*x
predict(stop_dist_model, newdata = data.frame(speed=1))
plot(dist~speed, data= cars, xlab = " Speed (in Miles per Hour)", ylab = " Stopping Distance (in Foot)", main = " Stopping Distance vs speed", pch = 20, cex = 2, col = "grey")
abline(stop_dist_model, lwd = 2, col = "purple")
abline(fitted(stop_dist_model), lwd = 2, col = "blue")
fitted(stop_dist_model)
str(fitted(stop_dist_model))
head(fitted(stop_dist_model))
names(fitted(stop_dist_model))
y=fitted(stop_dist_model$num)
y=fitted(stop_dist_model)
names(y)
summary(y)
nw_stop_dist_model(c(1:50, y))
nw_stop_dist_model=(c(1:50, y))
abline(fitted(nw_stop_dist_model), lwd = 2, col = "blue")
abline(nw_stop_dist_model, lwd = 2, col = "blue")
x=1:50
nw_stop_dist_model= c(x,y)
abline(nw_stop_dist_model, lwd = 2, col = "blue")
str(nw_stop_dist_model)
nw_stop_dist_model
nw_stop_dist_model=0
nw_stop_dist_model
nw_stop_dist_model=(c(x, y))
nw_stop_dist_model
nw_stop_dist_model=(x,y)
nw_stop_dist_model=x
nw_stop_dist_model
nw_stop_dist_model=y
y
clr
clearPushBack()
